# FEAT-007 — 要件の単一の正。実装・テスト・レビューはこれに従属する。
# 出典: docs/instructions.md（ankb 実装指示書）§10 ステップ7 / §7.1 / UC4
#
# 埋め方: 人間に質問する前に、AIが全項目を推測で埋め切ること。
# 人間の仕事は白紙を埋めることではなく、埋まったものを否定・修正することにある。
# 確信が持てない箇所は open_questions か assumptions に置き、空欄で残さない。
id: FEAT-007
title: "サーバMCP: 外部エージェントがユーザー本人の権限で読み書きする"
status: frozen # draft -> (spec-lint --gate freeze) -> frozen

intent: "翔が Claude Code での実装中に仕様や設計判断を参照できるように、ankb を Streamable HTTP
  のMCPサーバとして公開する。外部エージェントはOAuthで得たユーザー本人のトークンで動き、Web UI
  と同じ検索関数・同じRLSを通る。承認・公開範囲の変更・削除はツールとして提供しない。"

out_of_scope:
  - "リアルタイム共同編集（指示書 §11）"
  - "外部サービス（Slack / Google Drive）からの自動取り込み（指示書 §11）"
  - "view / edit より細かいロール（指示書 §11）"
  - "ネイティブモバイルアプリ（指示書 §11）"
  - "ノートの物理削除。v1 は status=archived のみ（指示書 §11）"
  - "stdio トランスポート"
  - "resolve_relation（承認）・公開範囲の変更・ノート削除のMCPツール（指示書 §7.1）"
  - "APIキー（長期トークン）による認証。認証はOAuthのみ"
  - "MCPのリソース・プロンプト機能。ツールだけを公開する"

assumptions:
  - id: AS-024
    statement: "MCPエンドポイントは Next.js の Route
      Handler（/mcp）として同じアプリに置き、@modelcontextprotocol/sdk の Streamable HTTP
      をステートレスで動かす。認可サーバは Supabase Auth の OAuth 2.1 サーバ機能を使い、ankb はリソースサーバとして
      Protected Resource Metadata を公開してトークン（Supabase の JWT）を検証する。受け取ったJWTをそのまま
      Supabase クライアントに渡し、RLS を効かせる"
    status: assumed
    owner: pb10005
    resolve_by: "2026-10-09"
  - id: AS-025
    statement: "エラーは isError: true の結果として返し、先頭テキストを JSON {code, message} とする。code は
      VALIDATION_ERROR | NOT_FOUND | FORBIDDEN | CONFLICT。閲覧できないノートは FORBIDDEN
      ではなく NOT_FOUND を返す（存在を漏らさない）"
    status: assumed
    owner: pb10005
    resolve_by: "2026-10-09"
  - id: AS-026
    statement: "list_pending_relations は、呼び出したユーザーが両方のノートの編集権限を持つ proposed の提案だけを返す"
    status: assumed
    owner: pb10005
    resolve_by: "2026-10-09"
  - id: AS-050
    statement: "MCP の create_note は visibility を受け付けず常に private で作成する。status は
      draft（既定）または active を受け付ける。update_note は title・body・effective_from と
      status の draft→active だけを受け付け、visibility・archived・superseded の指定は
      VALIDATION_ERROR とする（公開範囲の変更と削除は人間がUIで行う: §7.1）"
    status: assumed
    owner: pb10005
    resolve_by: "2026-10-09"
  - id: AS-054
    statement: "ask はユーザーあたり1時間20回までとし、超えた場合は RATE_LIMITED を返す（LLM
      利用料の暴走を防ぐ）。上限は環境変数 ANKB_ASK_RATE_LIMIT で変更できる"
    status: confirmed
    owner: pb10005
    resolve_by: "2026-10-09"
  - id: AS-059
    statement: "OAuth 経由で発行されたトークンを JWT クレーム client_id
      で識別し、resolve_relation・visibility の変更・note_share と member の書き込みを DB 側（RLS /
      RPC）で拒否する。Supabase Auth の OAuth 2.1 サーバが Claude Code
      の接続に必要な動的クライアント登録に対応していない場合は、ankb 自身が MCP 用の OAuth 認可エンドポイントを持ち、Supabase の
      JWT を client_id クレーム付きで発行し直す（OQ-003 を 2026-09-25 に人間が承認。§0 の権限に関わる変更）"
    status: confirmed
    owner: pb10005
    resolve_by: "2026-09-25"
  - id: AS-078
    statement: "get_note_lineage は SECURITY DEFINER の関数 public.note_lineage
      で置き換えの連鎖を両方向にたどり、呼び出し者が閲覧できるノートだけを返す（閲覧できない中間ノートは id・タイトルを出さずに飛ばす）。RLS
      の下では閲覧できない中間ノートとの関係が見えず、連鎖の先をたどれないため。FEAT-002 AC-091 の許可リストに note_lineage
      を加えた（2026-09-26 人間承認）"
    status: confirmed
    owner: pb10005
    resolve_by: "2026-10-10"

open_questions: []

acceptance:
  - id: AC-060
    kind: functional
    given: "OAuthで認可済みの翔のトークン"
    when: "tools/list を呼ぶ"
    then: "search_knowledge / ask / get_note / get_note_lineage /
      list_pending_relations / create_note / update_note / propose_relation
      の8件だけを返す"
    priority: must
    verify: test
    status: pass
    attempts: 0
    verified_at: 2026-09-26
    note: "実OAuthトークンで tools/list、8件の名前集合が完全一致（余分なし）。npx playwright test
      e2e/mcp.spec.ts: 13 passed (2026-09-26)"
    evidence: e2e/mcp.spec.ts:18

  - id: AC-062
    kind: functional
    given: "翔が閲覧できない美咲の private ノート P"
    when: "翔のトークンで search_knowledge（P に該当する語）と get_note(P の id) を呼ぶ"
    then: "search_knowledge の結果に P を含まず、get_note は存在しない id と同じ NOT_FOUND を返す"
    priority: must
    verify: test
    status: pass
    attempts: 0
    verified_at: 2026-09-26
    note: "索引済みの美咲private
      Pが翔の検索結果に無い(41)、get_note(P)がNOT_FOUNDかつ存在しないidとcontent完全一致(44-45)。npx
      playwright test e2e/mcp.spec.ts: 13 passed (2026-09-26)"
    evidence: e2e/mcp.spec.ts:41

  - id: AC-063
    kind: functional
    given: "翔が両方を閲覧できる active なノート2件"
    when: "propose_relation(type=supersedes) を呼ぶ"
    then: "state=proposed / proposed_by=agent の note_relation を作成する"
    priority: must
    verify: test
    status: pass
    attempts: 0
    verified_at: 2026-09-26
    note: "DB行で state=proposed / proposed_by=agent / type=supersedes を確認。npx
      playwright test e2e/mcp.spec.ts: 13 passed (2026-09-26)"
    evidence: e2e/mcp.spec.ts:56

  - id: AC-064
    kind: functional
    given: "AC-063 で作成した提案"
    when: "翔のトークンで resolve_relation 相当の操作（update_note で status=superseded、存在しないツール名
      resolve_relation の呼び出し）を試みる"
    then: "いずれもエラーを返し、提案の state は proposed のまま変わらない"
    priority: must
    verify: test
    status: pass
    attempts: 0
    verified_at: 2026-09-26
    note: "update_note(status=superseded)がisError(67)、未登録ツールresolve_relationもエラー(69\
      )、DBでstate=proposed(70)・note status=active(71)。npx playwright test
      e2e/mcp.spec.ts: 13 passed (2026-09-26)"
    evidence: e2e/mcp.spec.ts:67

  - id: AC-065
    kind: functional
    given: "(a) Authorization ヘッダ無し (b) 有効期限切れのJWT (c) 署名が不正なJWT (d) Web UI
      のログインで得たセッションJWT"
    when: "それぞれで POST /mcp を送る"
    then: "4ケースとも 401 と Protected Resource Metadata の URL を含む WWW-Authenticate
      ヘッダを返し、ツールを実行しない"
    priority: must
    verify: test
    status: pass
    attempts: 0
    verified_at: 2026-09-26
    note: "(a)-(d)の4ケースで401とresource_metadata付きWWW-Authenticate(93-94)、create_noteが\
      作られていないことをDBで確認(96)。期限切れJWTは本物の鍵で署名・client_id付きでexpのみ過去。npx playwright
      test e2e/mcp.spec.ts: 13 passed (2026-09-26)"
    evidence: e2e/mcp.spec.ts:93

  - id: AC-066
    kind: functional
    given: "翔のトークン"
    when: "create_note を visibility・status を指定せずに呼び、続けて visibility=workspace を指定して呼ぶ"
    then: "1回目は visibility=private / status=draft のノートを作成し、2回目は VALIDATION_ERROR
      を返してノートを作成しない"
    priority: must
    verify: test
    status: pass
    attempts: 0
    verified_at: 2026-09-26
    note: "1回目はDBでprivate/draft(107)、2回目はVALIDATION_ERROR(110)かつ同タイトル行0件(111)。npx
      playwright test e2e/mcp.spec.ts: 13 passed (2026-09-26)"
    evidence: e2e/mcp.spec.ts:107

  - id: AC-067
    kind: functional
    given: "翔に view で共有されたノート"
    when: "update_note を呼ぶ"
    then: "権限エラーを返し、ノートの本文は変わらない"
    priority: must
    verify: test
    status: pass
    attempts: 0
    verified_at: 2026-09-26
    note: "view共有ノートのupdate_noteがFORBIDDEN(120)、DB本文不変(121)。npx playwright test
      e2e/mcp.spec.ts: 13 passed (2026-09-26)"
    evidence: e2e/mcp.spec.ts:120

  - id: AC-068
    kind: functional
    given: "置き換えの連鎖 A→B→C のうち、翔は B を閲覧できない"
    when: "get_note_lineage(C) を呼ぶ"
    then: "B の id・タイトルを含まず、A と C を日付の昇順で並べた結果を返す"
    priority: must
    verify: test
    status: pass
    attempts: 0
    verified_at: 2026-09-26
    note: "confirmedのA<-B<-C連鎖でBはprivate。結果idが[A,C]の日付昇順(141)、Bのid・タイトルを含まない(142-14\
      3)。npx playwright test e2e/mcp.spec.ts: 13 passed (2026-09-26)"
    evidence: e2e/mcp.spec.ts:141

  - id: AC-069
    kind: functional
    given: "出張規程シナリオと美咲のセッション"
    when: "同じクエリで Web UI の検索API・サーバMCP の search_knowledge・WebMCP の search_knowledge
      を呼ぶ"
    then: "3つの入口の結果のうち hits / superseded_context / conflicts が一致する"
    priority: must
    verify: test
    status: pending
    attempts: 0

  - id: AC-070
    kind: functional
    given: "権限シナリオの全ユーザーのトークン"
    when: "8ツールすべてを、各ユーザーが閲覧できないノートの id または該当語を引数にして呼ぶ"
    then: "閲覧できないノートの id・title・本文断片・件数を含む返り値が1件も無い"
    priority: must
    verify: test
    status: pass
    attempts: 0
    verified_at: 2026-09-26
    note: "5ユーザー×8ツール（search2・ask・get_note3・lineage3・list_pending・update・propose・cr\
      eate）で不可視ノートのid・title・合言葉/金額断片が出ないこと(206-210)、不可視id指定のupdate/proposeがNOT_\
      FOUND(212-213)。件数はSearchResultに件数フィールドが無く、NOT_FOUNDの一様化で担保。npx playwright
      test e2e/mcp.spec.ts: 13 passed (2026-09-26)"
    evidence: e2e/mcp.spec.ts:206

  - id: AC-071
    kind: functional
    given: "README の接続手順"
    when: "Claude Code に `claude mcp add --transport http` で ankb を登録し、OAuth
      のログインを完了する"
    then: "Claude Code の search_knowledge 呼び出しが SearchResult を返す"
    priority: must
    verify: manual
    evidence: ""
    status: pending
    attempts: 0

  - id: AC-120
    kind: functional
    given: "翔が両方のノートの編集権限を持つ proposed の提案 R と、OAuth で得た翔のアクセストークン"
    when: "そのトークンで Supabase の REST API から rpc/resolve_relation(R, confirm) と notes の
      visibility 更新を直接呼ぶ"
    then: "両方ともエラーを返して拒否し、R は proposed、visibility は変わらない"
    priority: must
    verify: test
    status: pass
    attempts: 0
    verified_at: 2026-09-26
    note: "OAuthトークンでREST直叩き:
      rpc/resolve_relationとnotes.visibility更新がともにerror(179-180)、DBでR=proposed(1\
      81)・visibility=workspace(182)。拒否はDB側
      is_oauth_client()（schema_rls.sql:119,200,294）。npx playwright test
      e2e/mcp.spec.ts: 13 passed (2026-09-26)"
    evidence: e2e/mcp.spec.ts:179

  - id: AC-123
    kind: functional
    given: "翔のトークン"
    when: "tools/list を呼び、search_knowledge と ask を実行する"
    then: "8ツールすべてに outputSchema を含み、search_knowledge は SearchResult 型、ask は Answer
      型（§7.3）のスキーマ検証を通る structuredContent を返す"
    priority: must
    verify: test
    status: pass
    attempts: 0
    verified_at: 2026-09-26
    note: "全ツールoutputSchema定義(25)、search_knowledge/askのstructuredContentがSearchResu\
      ltSchema/AnswerSchemaを通過(28,31)。スキーマは実装側定義だがdocs
      §7.3の型とフィールド一致を目視確認（Answerはformatを追加）。ask合成器はテスト用Echo。npx playwright test
      e2e/mcp.spec.ts: 13 passed (2026-09-26)"
    evidence: e2e/mcp.spec.ts:25

  - id: AC-124
    kind: functional
    given: "active なノート X と、X に contradicts(proposed) で結ばれた閲覧可能なノート Y"
    when: "get_note(X) を呼ぶ"
    then: "status・possibly_outdated と conflicts:[{note_ids:[X,Y],
      relation_state:'proposed'}] を含む structuredContent を返す"
    priority: must
    verify: test
    status: pass
    attempts: 0
    verified_at: 2026-09-26
    note: "status=active・possibly_outdated=false・conflicts=[{note_ids:[X,Y],relatio\
      n_state:'proposed'}]（配列長も一致）をtoMatchObjectで検証。npx playwright test
      e2e/mcp.spec.ts: 13 passed (2026-09-26)"
    evidence: e2e/mcp.spec.ts:153
